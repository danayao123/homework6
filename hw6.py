def check_task_completion(hours_spent, target_hours):
    """
    check whether the actual study hour meet the target
    
    parameter:
        hours_spent (float): the actual study hour.
        target_hours (float): the target study hour.
        
    return:
        bool: if actual time >= target time，return True；else return False.
    """
    pass 

def format_furniture_name(name):
    """
    Format the furniture names so that the first letter is capitalized and the remaining letters are lowercase.
    
    parameter:
        name (str): original furniture name（ "CHAIR" / "table").
        
    return:
        str: capitalized name（ "Chair" / "Table").
    """
    pass
